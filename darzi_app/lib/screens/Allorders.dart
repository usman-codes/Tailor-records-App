import 'package:flutter/material.dart';

class AllOrdersScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('All Orders'),
      ),
      body: Center(
        child: Text('All Orders Screen'),
      ),
    );
  }
}
