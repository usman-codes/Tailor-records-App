import 'package:flutter/material.dart';

class AllCustomersScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('All Customers Screen'),
      ),
    );
  }
}
