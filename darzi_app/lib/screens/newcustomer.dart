import 'package:flutter/material.dart';

class NewCustomerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('New Customer'),
      ),
      body: Center(
        child: Text('New Customer Screen'),
      ),
    );
  }
}
